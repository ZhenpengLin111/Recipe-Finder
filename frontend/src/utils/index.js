// Middleware tool module function
import { request } from './request'
import { getToken, setToken, removeToken, decodedUser } from './token'

export {
  request,
  getToken,
  setToken,
  removeToken,
  decodedUser
}